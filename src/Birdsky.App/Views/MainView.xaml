<local:MainViewBase
    x:Class="Birdsky.App.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Birdsky.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:feed="using:FishyFlip.Lexicon.App.Bsky.Feed"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<ListView SelectionMode="None" ItemsSource="{x:Bind ViewModel.Feed}">
		<ListView.ItemTemplate>
			<DataTemplate x:DataType="feed:FeedViewPost">
				<StackPanel Margin="4" Spacing="8">
					<TextBlock Text="{Binding Record.Text}" TextWrapping="WrapWholeWords" />
					<TextBlock Text="{x:Bind Post.Author.DisplayName}" FontWeight="Bold" />
				</StackPanel>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</local:MainViewBase>
